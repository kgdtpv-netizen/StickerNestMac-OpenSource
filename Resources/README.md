# Resources

No runtime policy JSON is tracked or bundled from this directory.

Private profiles, production-derived layout or stagger policies, production QR
mappings, customer artwork, and raw benchmark inputs must remain outside Git.
The repository ignores the known local policy filenames. `build.zsh` uses an
explicit runtime-file allowlist instead of copying this directory.
